Title card: Microsoft Defender quarantined a file, and that quarantine was the exploit. RoguePlanet turns a standard user into SYSTEM on a fully updated Windows 11, with no CVE and no patch. A red shield fractures in the dark.

Nightmare Eclipse released RoguePlanet on June 10, with no CVE, no patch, and Windows 11 Pro confirmed vulnerable; multiple researchers reproduced it. A field of dark slabs, one pulsing red while the rest stay dormant.

A standard user with no admin rights gains a SYSTEM shell on a fully updated machine, using Defender itself as the lever, and the signature only flags the compiled sample. A scanning plane sweeps a smooth object and fractures into red.

An oplock pauses Defender mid-scan, then a junction swap makes Defender quarantine a SYSTEM-owned file into a folder the user controls. Two threads race in the dark, one seizing the resource a moment before the other.

Diagram of the RoguePlanet attack chain in eight steps: dual-mode entry by named pipe, I/O saturation, ISO mount and fake System32, EICAR plus oplock, junction swap, payload injection, the WER task, ending at NT AUTHORITY SYSTEM.

There is no patch to apply and no toggle to flip; it is a design-level race between Defender, NTFS junctions, and Volume Shadow Copy, and the signature only catches one compiled binary. A grid of sealed cells, one cracking open and leaking red.

Hunt the chain, not the binary: a named pipe called RoguePlanet from a non-system process, wermgr.exe running outside System32, and SYSTEM spawning conhost.exe with no command prompt in its tree. A protective dome intact on one side and fractured on the other.

Your antivirus is not just a scanner. It is a SYSTEM-level file mover, and its quarantine can be raced. A shield with one half intact and the other half fractured, red particles escaping.

The call to action asks whether your EDR could catch a SYSTEM shell that came from Defender's own quarantine. A single faint pulse of light on a clean dark end card.
