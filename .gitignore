# ── Dependencies ────────────────────────────────────────────────
node_modules/
.pnp/
.pnp.js

# ── Build / cache output ────────────────────────────────────────
dist/
build/
.vite/
.cache/
# Remotion
out/
.remotion/

# ── Logs ────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Environment / secrets ───────────────────────────────────────
.env
.env.*
!.env.example
renderer/.secrets/

# ── Personal voice reference clips (used for voice cloning; local-only) ──
_voiceref/

# ── Claude Code local state ─────────────────────────────────────
.claude/settings.local.json

# ── OS / editor cruft ───────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/

# ── Temp ────────────────────────────────────────────────────────
*.tmp
tmp/

# ── Other ────────────────────────────────────────────────────────
assets/Content Dashboard Plan Reference/
.kanbots/

# ── Pipeline ────────────────────────────────────────────────────────
# Rendered post output (carousel PNGs, reel MP4, captions/sources/checklists) is
# upload-ready media, not source, and should never land in the repo.
pipeline/renders/

# ── Tooling caches ──────────────────────────────────────────────────
.tokensave/