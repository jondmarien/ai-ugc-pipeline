The quiet mono account handle / watermark, top-left on every slide.

```jsx
<BrandMark />
<BrandMark handle="@chron0s_cyb3r_w0rld.ai" />
```

Muted, uppercase, wide tracking. Never competes with the headline. Defaults to the house handle.
