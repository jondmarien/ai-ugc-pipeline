The pill CTA / swipe cue — the only loud call-to-action on a slide.

```jsx
<Chip>Swipe →</Chip>
<Chip variant="outline">Save + Follow</Chip>
```

`solid` (default) fills with the accent and uses near-black ink; `outline` is the quiet version. Mono, uppercase, wide tracking. Use one per slide max.
