The quiet muted clarifier under a headline; one sentence, never a paragraph.

```jsx
<Subline>Prompt injection isn't always typed by the user.</Subline>
```

Inter 500, muted, capped at 90% width. Renders nothing if empty, so it's safe to pass an optional value.
